@model IEnumerable<Jarvis.Models.SMSDemandFRD>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.FRD.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SMSDemand.SMSCode.ReferenceCode)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SMSDemand.SMSCode.CodeDescription)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.FRD.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SMSDemand.SMSCode.ReferenceCode)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SMSDemand.SMSCode.CodeDescription)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.ID }) |
            @Html.ActionLink("Details", "Details", new { id = item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.ID })
        </td>
    </tr>
}

</table>
